安装
====
.. include:: README_ZH.md
    :start-after: ## 安装
    :end-before: * **注意**：
    :parser: myst_parser.sphinx_

* **注意**：
  * 使用这种方法安装时，只有 `data_juicer` 中的基础的 API 和2个基础工具
    （数据 `处理 <quick-start_ZH.html#id2>`_与 `分析 (quick-start_ZH.html#id4>`_）可以使用。如需更定制化地使用完整功能，建议 `从源码进行安装 <#id2>`_)。
  * pypi 的发布版本较源码的最新版本有一定的滞后性，如需要随时跟进 `data_juicer` 的最新功能支持，建议 `从源码进行安装 <#id2>`_)。

使用 Docker 安装
^^^^^^^^^^^^^^^^
.. include:: README_ZH.md
    :start-after: ### 使用 Docker 安装
    :end-before: ## 快速上手
